## Packages
framer-motion | Essential for premium, smooth page transitions and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to intuitively merge Tailwind classes without style conflicts

## Notes
- The app uses `react-router-dom` for routing as per the existing `App.tsx` structure.
- Core brand colors (`palop-green` and `palop-blue`) are mapped securely into the new Tailwind v4 theme to prevent breaking existing un-migrated components.
- The design introduces a premium "glassmorphism" and modern aesthetic with deep, colorful shadows and dynamic layout spacing.
